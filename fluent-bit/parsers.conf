[PARSER]
    # nginx schreibt bereits JSON-Zeilen (siehe log_format json_lehrplan).
    Name        nginx_json
    Format      json
    # Kein Time_Key: Fluent Bit nutzt die Einlesezeit als @timestamp (sekundengenau
    # ausreichend). nginx_time bleibt als Feld erhalten.
